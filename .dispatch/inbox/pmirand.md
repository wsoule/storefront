# Inbox

Captured, not committed. Edit this file freely — add a `- [ ] your thought`
line anywhere and Dispatch will pick it up.

- [ ] (idea) price discount checks through one shared server-side helper instead of duplicating per endpoint ^in-b1
- [ ] (note) redis cache TTL should match session TTL once this lands → t-1d77e5 ^in-b2
- [x] (task) fix hyphenated SKU search → t-0c9b88 ^in-b3
